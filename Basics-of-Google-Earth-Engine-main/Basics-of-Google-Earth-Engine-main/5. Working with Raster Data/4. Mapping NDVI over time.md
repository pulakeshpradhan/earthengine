```javascript
/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var forest = /* color: #d63000 */ee.Geometry.Point([73.72201900196919, 18.399550459934037]),
    field = /* color: #98ff00 */ee.Geometry.Point([75.10243168103595, 18.042112635341944]),
    builtup = /* color: #0b4a8b */ee.Geometry.Point([73.93292148494648, 18.50084511858724]),
    waterbody = /* color: #ffc82d */ee.Geometry.Point([73.76326802080158, 18.433624142364568]),
    L8 = ee.ImageCollection("LANDSAT/LC08/C02/T1_TOA");
/***** End of imports. If edited, may not auto-convert in the playground. *****/
var VisParams = {min: 0, max: 0.3,bands: ['B4', 'B3', 'B2']};
Map.setCenter(73.8,18.5,10);
 
//Filter the image collection over the year 2022
var filtered = L8.filterDate('2022-01-01', '2023-01-01');
   
// //Define a function to calculate NDVI for an image and add it as a band
// var addNDVI = function(img) {
//   var ndvi = img.normalizedDifference(['B5','B4']).rename('ndvi');
//   return img.addBands(ndvi); 
//   };
  
// //Map user defined function over filtered Image Collection to calculate NDVI and add it as a band 
// var withNDVI = filtered.map(addNDVI);
// //print(withNDVI);
// // print(withNDVI.size());
// //Chart NDVI over time at any point of interest
// print(ui.Chart.image.series(withNDVI.select('ndvi'),waterbody));
