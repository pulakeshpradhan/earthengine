```javascript
// Search for WWF Hydrosheds Basins Feature Collection in the DataCatalog
 
/*HydroSHEDS is a mapping product that provides hydrographic information for regional and 
global-scale applications. It offers a suite of geo-referenced datasets
(vector and raster) at various scales, including river networks, watershed boundaries, drainage
directions, and flow accumulations. HydroSHEDS is based on elevation data obtained in 2000 by 
NASA's Shuttle Radar Topography Mission (SRTM). This dataset provides polygons of nested, 
hierarchical watersheds. The watersheds range from level 1 (coarse) to level 12 (detailed) */

var HydroshedsLevel6 = ee.FeatureCollection("WWF/HydroSHEDS/v1/Basins/hybas_12");
Map.addLayer(HydroshedsLevel6);
 
// // Inspect the Feature Collection using Inspector tab
// //Select any sub-basin using its 'HYBAS_ID' property and add it onto Map Display

// var mulaMutha = ee.Feature(HydroshedsLevel6.filter(ee.Filter.eq('HYBAS_ID', 4061067580)).first());
// print(mulaMutha);
// Map.centerObject(mulaMutha,8);
// Map.addLayer(mulaMutha, {color:'blue'},'Mula Mutha sub-Basin');

// var subBasinArea = mulaMutha.geometry().area().divide(1e6);
// print('Sub-basin Area in Sq. kms. :', subBasinArea);


// /*-----------------------------------------------------------------------------------------------*/


// var GaulLevel2 = ee.FeatureCollection("FAO/GAUL_SIMPLIFIED_500m/2015/level2");

// //Filter by Country Name 
// var indiaAdm2Map = GaulLevel2.filter(ee.Filter.eq('ADM0_NAME', 'India'));
// print(indiaAdm2Map.size());
// Map.addLayer(indiaAdm2Map, {},'India Admin Map level 2');

// //Filter by State Name 
// var maharashtraAdm2Map = indiaAdm2Map.filter(ee.Filter.eq('ADM1_NAME', 'Maharashtra'));
// var mystyle = {color:'blue',fillColor:'ffffffff', width:1}; 
// Map.addLayer(maharashtraAdm2Map.style(mystyle), {},'Maharashtra');

// //Mapping function to be map over featurecollection
// var addArea = function(feature){
//     var area = feature.geometry().area().divide(1000000);
//     return feature.set({areaSqKm:area});
// }


// var maharashtraAdm2MapWithArea = maharashtraAdm2Map.map(addArea);
// // print(maharashtraAdm2Map.size());
// // print(maharashtraAdm2MapWithArea.size());


// print(maharashtraAdm2Map);
// print(maharashtraAdm2MapWithArea);

// // Compute sum of specified properties( District Area) using reduceColumns() method

// var maharashtraTotalArea = maharashtraAdm2MapWithArea
//     .reduceColumns({
//       reducer: ee.Reducer.sum(),
//       selectors: ['areaSqKm']
//     });

// print('Total Area of Maharashtra as per GAUL dataset (in Sq.km) :',maharashtraTotalArea.get('sum'));


// /*
// CALCULATE RAINFALL STATISTICS IN MULTIPLE REGIONS AND EXPORT THE RESULTS TO GDRIVE
// ----------------------------------------------------------------------------------
// */

// var chirps = ee.ImageCollection("UCSB-CHG/CHIRPS/DAILY");

// var GAULLevel2 = ee.FeatureCollection("FAO/GAUL_SIMPLIFIED_500m/2015/level2");

// //Filter by Country Name
// var indiaAdm2Map = GAULLevel2.filter(ee.Filter.eq('ADM0_NAME', 'India'));

// //Filter by State Name 
// var maharashtraAdm2Map = indiaAdm2Map.filter(ee.Filter.eq('ADM1_NAME', 'Maharashtra'));

// // Filter Chirps Daily Data for the  year 2022
// var chirpsFiltered = chirps.filterDate('2022-01-01','2023-01-01');

// //Calculate cumulative rainfall for the year 2022
// var totalRF = chirpsFiltered.sum();

// //Calculate Average Rainfall for all districts of Mahrashtra for the year 2022

// var DistrictWiseAvgRF = totalRF.reduceRegions({
//   collection: maharashtraAdm2Map,
//   reducer: ee.Reducer.mean(),
//   scale: 5000
// });

// // print(maharashtraAdm2Map);
// print(DistrictWiseAvgRF);//redueRegions() returns a FeatureCollection with added band

// //Export computed rainfall statistics to the Drive

// Export.table.toDrive({
//   collection : DistrictWiseAvgRF,
//   description: 'DistrictWiseAvgRFin2020',
//   selectors :['ADM2_NAME', 'mean']
// });
