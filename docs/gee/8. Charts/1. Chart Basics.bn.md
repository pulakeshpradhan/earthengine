# গুগল আর্থ ইঞ্জিনে চার্টসমূহ

## ১. ইমেজ-ভিত্তিক চার্ট

### টাইম সিরিজ বিশ্লেষণ

- `ui.Chart.image.series`: সময়ের সাথে একক অঞ্চল
- `ui.Chart.image.seriesByRegion`: সময়ের সাথে একাধিক অঞ্চল
- `ui.Chart.image.doySeries`: একক অঞ্চলের বছরের-দিন প্যাটার্ন
- `ui.Chart.image.doySeriesByYear`: একক অঞ্চলের জন্য বছর তুলনা
- `ui.Chart.image.doySeriesByRegion`: অঞ্চল জুড়ে বছরের-দিন প্যাটার্ন

### সাধারণ প্যারামিটার

- `scale`: মিটারে স্থানিক রেজোলিউশন
- `reducer`: পিক্সেল মান সমষ্টিকরণ ফাংশন (যেমন- mean, max, min)
- `region/regions`: বিশ্লেষণের জন্য আগ্রহের এলাকা(সমূহ)
- `xProperty`: x-অক্ষ মানের জন্য প্রপার্টি
- `seriesProperty`: একাধিক সিরিজ আলাদা করার প্রপার্টি

### কাস্টমাইজেশন অপশন

```javascript
.setOptions({
  title: 'চার্ট শিরোনাম',
  hAxis: {title: 'X-অক্ষ লেবেল', titleTextStyle: {italic: false, bold: true}},
  vAxis: {title: 'Y-অক্ষ লেবেল', titleTextStyle: {italic: false, bold: true}},
  lineWidth: 2,
  colors: ['hexcolor1', 'hexcolor2'],
  curveType: 'function'  // মসৃণ রেখার জন্য
})
```

---

### **১. `ui.Chart.image.series`**

#### বর্ণনা

এই চার্ট দেখায় একটি নির্দিষ্ট অঞ্চলে (যেমন- বন) সময়ের সাথে গাছপালা সূচক (NDVI ও EVI) কীভাবে পরিবর্তিত হয়।

```javascript
var forest = ee.FeatureCollection('projects/google/charts_feature_example')
                 .filter(ee.Filter.eq('label', 'Forest'));
var vegIndices = ee.ImageCollection('MODIS/061/MOD13A1')
                     .filter(ee.Filter.date('2010-01-01', '2020-01-01'))
                     .select(['NDVI', 'EVI']);

var chart = ui.Chart.image.series({
  imageCollection: vegIndices, region: forest,
  reducer: ee.Reducer.mean(), scale: 500, xProperty: 'system:time_start'
}).setOptions({
  title: 'বনের জন্য তারিখ অনুসারে গড় গাছপালা সূচক মান',
  hAxis: {title: 'তারিখ'}, vAxis: {title: 'গাছপালা সূচক (x1e4)'},
  lineWidth: 5, colors: ['e37d05', '1d6b99']
});
print(chart);
```

---

### **২. `ui.Chart.image.seriesByRegion`**

#### বর্ণনা

এই চার্ট একাধিক অঞ্চলে (যেমন- বন, মরুভূমি, তৃণভূমি) সময়ের সাথে NDVI মান তুলনা করে।

```javascript
var ecoregions = ee.FeatureCollection('projects/google/charts_feature_example');
var chart = ui.Chart.image.seriesByRegion({
  imageCollection: vegIndices, band: 'NDVI', regions: ecoregions,
  reducer: ee.Reducer.mean(), scale: 500,
  seriesProperty: 'label', xProperty: 'system:time_start'
}).setOptions({
  title: 'তারিখ অনুসারে গড় NDVI মান',
  lineWidth: 5, colors: ['f0af07', '0f8755', '76b349']
});
print(chart);
```

---

### **৩. `ui.Chart.image.doySeries`**

#### বর্ণনা

এই চার্ট একাধিক বছর জুড়ে একক অঞ্চলের জন্য **বছরের প্রতিটি দিনে** গড় NDVI ও EVI মান প্লট করে। মৌসুমী গাছপালা প্রবণতা চিহ্নিত করতে সাহায্য করে।

```javascript
var grassland = ee.FeatureCollection('projects/google/charts_feature_example')
                    .filter(ee.Filter.eq('label', 'Grassland'));

var chart = ui.Chart.image.doySeries({
  imageCollection: vegIndices, region: grassland,
  regionReducer: ee.Reducer.mean(), scale: 500,
  yearReducer: ee.Reducer.mean(), startDay: 1, endDay: 365
}).setOptions({title: 'তৃণভূমির বছরের দিন অনুসারে গড় গাছপালা সূচক মান'});
print(chart);
```

---

### **৪. `ui.Chart.image.doySeriesByYear`**

#### বর্ণনা

এই চার্ট বিভিন্ন বছরে নির্দিষ্ট অঞ্চলের NDVI প্রবণতা তুলনা করে।

```javascript
var vegIndices = ee.ImageCollection('MODIS/061/MOD13A1')
  .filter(ee.Filter.or(
    ee.Filter.date('2012-01-01', '2013-01-01'),
    ee.Filter.date('2019-01-01', '2020-01-01')))
  .select(['NDVI', 'EVI']);

var chart = ui.Chart.image.doySeriesByYear({
  imageCollection: vegIndices, bandName: 'NDVI', region: grassland,
  regionReducer: ee.Reducer.mean(), scale: 500,
  sameDayReducer: ee.Reducer.mean(), startDay: 1, endDay: 365
}).setOptions({title: 'তৃণভূমির বছরের দিন অনুসারে গড় NDVI মান'});
print(chart);
```

---

### **৫. `ui.Chart.image.doySeriesByRegion`**

#### বর্ণনা

এই চার্ট একটি বছরে বিভিন্ন অঞ্চলে (যেমন- বন, মরুভূমি, তৃণভূমি) গড় NDVI মান তুলনা করে। মৌসুমী গাছপালা বৈচিত্র্য দেখায়।

```javascript
var chart = ui.Chart.image.doySeriesByRegion({
  imageCollection: vegIndices, bandName: 'NDVI', regions: ecoregions,
  regionReducer: ee.Reducer.mean(), scale: 500,
  yearReducer: ee.Reducer.mean(), seriesProperty: 'label',
  startDay: 1, endDay: 365
}).setOptions({title: 'বছরের দিন অনুসারে গড় NDVI মান'});
print(chart);
```

---
[Pulakesh Pradhan](https://wa.me/918617812861) দ্বারা তৈরি
