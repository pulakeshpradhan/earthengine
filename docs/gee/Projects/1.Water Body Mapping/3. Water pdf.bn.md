## পানি পৃষ্ঠ ক্ষেত্রফল চার্টিং

##### [https://code.earthengine.google.co.in/9e82406b1579173e08c89a7996133dba](https://code.earthengine.google.co.in/9e82406b1579173e08c89a7996133dba)

---
এই স্ক্রিপ্ট সেন্টিনেল-২ ইমেজারি ব্যবহার করে অধ্যয়ন এলাকায় পানি বিস্তার ক্ষেত্রফল বিশ্লেষণের জন্য গুগল আর্থ ইঞ্জিন (GEE) ব্যবহার করে।

### **ধাপে ধাপে ব্যাখ্যা:**

#### ১. **আগ্রহের অঞ্চল (ROI) ও স্যাটেলাইট ডেটা সংজ্ঞায়িত করা**

```javascript
var studyArea = 
    ee.Geometry.Polygon(
        [[[85.76361517120263, 20.52393128133101],
          [85.76361517120263, 20.41553715846105],
          [85.95621923614404, 20.41553715846105],
          [85.95621923614404, 20.52393128133101]]], null, false);
var S2 = ee.ImageCollection("COPERNICUS/S2_SR");
```

* **`studyArea`** হলো অধ্যয়ন এলাকার FeatureCollection, যা ভৌগোলিক আগ্রহের এলাকা (ROI) প্রতিনিধিত্ব করে।
* **`S2`** সেন্টিনেল-২ সারফেস রিফ্লেক্ট্যান্স (SR) ImageCollection বোঝায়।

#### ২. **ইমেজ কালেকশন ফিল্টার করা**

```javascript
var S2filtered = S2.filterDate('2020-10-01', '2021-06-01')
                   .filterBounds(studyArea)
                   .filter(ee.Filter.lt('CLOUDY_PIXEL_PERCENTAGE',30));
```

* **অক্টোবর ১, ২০২০ থেকে জুন ১, ২০২১** এর মধ্যে ইমেজ ফিল্টার করে।
* শুধুমাত্র **৩০% এর কম মেঘাচ্ছন্নতা** যুক্ত ইমেজ নির্বাচন করে।

#### ৩. **NDWI ব্যান্ড যোগ করা**

```javascript
var addNDWI = function(inputImage) {
  var ndwi = inputImage.normalizedDifference(['B3','B8']).rename('ndwi');
  return inputImage.addBands(ndwi); 
};
var imagesWithNDWI = S2filtered.map(addNDWI);
```

* NDWI (Normalized Difference Water Index) সেন্টিনেল-২ এর **গ্রিন (B3)** ও **নিয়ার-ইনফ্রারেড (B8)** ব্যান্ড ব্যবহার করে গণনা করা হয়।

#### ৪. **পানির ক্ষেত্রফল গণনা**

```javascript
var waterAr = function(inputImage) {
  var waterMask = inputImage.select('ndwi').gte(ndwiThreshold);
  var pixelArea = ee.Image.pixelArea();
  var waterpixels = pixelArea.updateMask(waterMask);
  var waterArea = waterpixels.reduceRegion({
    reducer: ee.Reducer.sum(), geometry: studyArea,
    crs: inputImage.select('B3').projection(), maxPixels: 1e8, bestEffort: true
  });
  var areaImage = ee.Image(waterArea.getNumber('area').divide(1e6)).rename('waterspreadarea'); 
  return inputImage.addBands(areaImage);
};
```

* পানি পিক্সেলের ক্ষেত্রফল যোগ করে বর্গ কিলোমিটারে রূপান্তর করে।

#### ৫. **টাইম সিরিজ চার্ট তৈরি**

```javascript
var chart = ui.Chart.image.seriesByRegion(areaSeries, studyArea, ee.Reducer.mean(), 
  'waterspreadarea', 10, 'system:time_start',' ')
  .setChartType('ColumnChart')
  .setOptions(title);
print(chart);
```

#### ৬. **মেঘাচ্ছন্নতার চ্যালেঞ্জ**

```javascript
// বর্ষা মৌসুমে মেঘাচ্ছন্নতার কারণে পানি বিস্তার ক্ষেত্রফলের মান প্রতিনিধিত্বমূলক নাও হতে পারে
```

![ee-chart (1)](https://github.com/user-attachments/assets/bc8e72f5-9844-4d6e-8d43-d360e2017185)

[Pulakesh Pradhan](https://www.linkedin.com/in/pulakeshpradhan/) দ্বারা তৈরি
