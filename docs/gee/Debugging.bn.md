# গুগল আর্থ ইঞ্জিন ডিবাগিং

---

### 🤔 **নোট:**

এই পয়েন্টগুলো আর্থ ইঞ্জিনে কার্যকর ডিবাগিংয়ের জন্য সাধারণ ডিবাগিং পরিস্থিতি ও টুলগুলো বুঝতে সাহায্য করে।

- **সিনট্যাক্স এরর (Syntax Errors)**: সিনট্যাক্স এরর হয় যখন কোড জাভাস্ক্রিপ্ট বা পাইথনের সিনট্যাক্স নিয়ম অনুসরণ করে না, আর্থ ইঞ্জিন প্রায়ই এক্সিকিউশনের আগেই এগুলো হাইলাইট করে দেয়।

  ```javascript
  // সিনট্যাক্স এরর উদাহরণ: ক্লোজিং প্যারেনথেসিস নেই
  var image = ee.Image('LANDSAT/LC08/C01/T1_TOA/LC08_044034_20140318'
  print(image);
  // সমাধান
  var imageCorrect = ee.Image('LANDSAT/LC08/C01/T1_TOA/LC08_044034_20140318');
  print(imageCorrect);
  ```

- **ক্লায়েন্ট-সাইড এরর:** সিনট্যাক্টিকালি সঠিক কোডেও লজিক ইস্যু বা ভ্যারিয়েবল স্কোপ সীমাবদ্ধতার কারণে এরর হতে পারে (যেমন- undefined ভ্যারিয়েবল বা ভুল মেথড কল)।

  ```javascript
  // উদাহরণ: Undefined ভ্যারিয়েবল এরর
  var area = length * width; // 'length' এবং 'width' undefined
  // সমাধান
  var length = 10;
  var width = 5;
  var areaCorrect = length * width;
  ```

- **অজানা অবজেক্ট টাইপ কাস্টিং:** আর্থ ইঞ্জিন যখন একটি ভ্যারিয়েবলের টাইপ চিনতে পারে না তখন প্রায়ই এরর হয়। **সমাধান:** অবজেক্টকে স্পষ্টভাবে কাস্ট (Cast) করুন।

  ```javascript
  // উদাহরণ: পরিচিত টাইপে কাস্ট করা
  var feature = ee.Feature(null, {name: 'Test Feature'});
  var area = feature.get('area'); // 'area' না থাকলে এরর হবে
  ```

- **ডিবাগিং পদ্ধতি:** মধ্যবর্তী মানগুলো পরীক্ষা করতে ও সমস্যা চিহ্নিত করতে `print()`, `aside()`, এবং `Map.addLayer()` ব্যবহার করুন। ফাংশনগুলো আলাদা করে কালেকশনের পৃথক এলিমেন্টে পরীক্ষা করুন।

  ```javascript
  // উদাহরণ: ডিবাগিংয়ে `aside()` ব্যবহার
  var image = ee.Image('LANDSAT/LC08/C01/T1_TOA/LC08_044034_20140318');
  var bandNames = image.bandNames().aside(print); // পরিদর্শনের জন্য ব্যান্ড নাম প্রিন্ট করে
  ```

### ⚠️ সতর্কতা

আর্থ ইঞ্জিনের অনন্য সার্ভার-ক্লায়েন্ট স্ট্রাকচারের কারণে এরর এড়াতে ডেভেলপমেন্টে এই পয়েন্টগুলোতে বিশেষ যত্ন নিতে হবে।

- **সার্ভার-সাইড এরর:** আর্থ ইঞ্জিনে এরর প্রায়ই ইমেজে অস্তিত্বহীন ব্যান্ড অ্যাক্সেস করতে গেলে বা অসমর্থিত ডেটা টাইপে অপারেশন করতে গেলে হয়।

  ```javascript
  // উদাহরণ: অস্তিত্বহীন ব্যান্ড অ্যাক্সেস
  var image = ee.Image('COPERNICUS/S2/20190830T104031_20190830T104033_T31TFJ');
  var ndvi = image.normalizedDifference(['B4', 'B8']); // সেন্টিনেল-২ NDVI
  print(ndvi); // নিশ্চিত করুন দুটি ব্যান্ডই আছে
  ```

- **সার্ভার-সাইড অবজেক্টের অপরিবর্তনীয়তা:** আর্থ ইঞ্জিনে অবজেক্টগুলো অপরিবর্তনীয় (Immutable)। পরিবর্তন করতে হলে, বিদ্যমান অবজেক্ট পরিবর্তনের পরিবর্তে নতুন ভ্যারিয়েবলে অ্যাসাইন করতে হবে।

  ```javascript
  // উদাহরণ: অপারেশনের পর নতুন ভ্যারিয়েবলে অ্যাসাইন
  var image = ee.Image('LANDSAT/LC08/C01/T1_TOA/LC08_044034_20140318');
  var scaledImage = image.multiply(2); // নতুন ভ্যারিয়েবলে অ্যাসাইন
  ```

- **স্কেলিং এরর:** "computation timed out" বা "user memory limit exceeded" এর মতো এরর বৃহৎ-মাত্রার গণনার কারণে হয়। নির্ণয় করতে প্রোফাইলার পরীক্ষা করুন এবং প্রয়োজনে অপারেশন সরলীকরণ করুন।

  ```javascript
  // উদাহরণ: স্কেলিং এরর এড়াতে রিজিয়ন বা স্কেল সামঞ্জস্য করুন
  var reducedRegion = image.reduceRegion({
    reducer: ee.Reducer.mean(),
    geometry: image.geometry(),
    scale: 1000 // গণনা পরিচালনার জন্য স্কেল সামঞ্জস্য করুন
  });
  print(reducedRegion);
  ```

### ❌ পরিহার করুন

এই সাধারণ ভুলগুলো এড়িয়ে চলুন যা অপ্রত্যাশিত ফলাফল বা এক্সিকিউশন ব্যর্থতার কারণ হতে পারে।

- **ক্লায়েন্ট ও সার্ভার ফাংশন মিশানো:** আর্থ ইঞ্জিন (সার্ভার-সাইড) অবজেক্টের সাথে জাভাস্ক্রিপ্ট (ক্লায়েন্ট-সাইড) অপারেশন মিশানো এড়িয়ে চলুন।

  ```javascript
  // ভুল: ক্লায়েন্ট-সাইড ও সার্ভার-সাইড মিশানো
  var clientNumber = 2;
  var eeNumber = ee.Number(10);
  var result = eeNumber * clientNumber; // এরর হবে
  
  // সমাধান: ক্লায়েন্ট-সাইড ভ্যারিয়েবলকে সার্ভার-সাইডে রূপান্তর করুন
  var resultCorrect = eeNumber.multiply(ee.Number(clientNumber));
  ```

- **ব্রাউজার লক (`for` লুপ ও `getInfo()` পরিহার করুন):** বড় ডেটায় `getInfo()` ব্যবহার করলে ব্রাউজার ফ্রিজ হতে পারে। আর্থ ইঞ্জিন কালেকশনে `for` লুপ এড়িয়ে চলুন কারণ এটি ক্লায়েন্ট-সাইড ইভ্যালুয়েশন জোর করে।

  ```javascript
  // ভুল: বড় কালেকশনে getInfo() ব্যবহার
  var collection = ee.ImageCollection('LANDSAT/LC08/C01/T1_TOA');
  var size = collection.size().getInfo(); // ব্রাউজার ফ্রিজ হতে পারে
  
  // সমাধান: বড় কালেকশনে getInfo() এড়িয়ে চলুন
  collection.size().aside(print);
  ```

- **ম্যাপড ফাংশনে ক্লায়েন্ট-সাইড অপারেশন:** ম্যাপড ফাংশনে শুধুমাত্র সার্ভার-সাইড অপারেশন থাকে তা নিশ্চিত করুন, কারণ ম্যাপড ফাংশনের ভেতরে ক্লায়েন্ট-সাইড অপারেশন এরর সৃষ্টি করবে।

- **প্রসিডিউরাল এরর:** অসামঞ্জস্যপূর্ণ ফাংশন প্রয়োগ, যেমন প্রয়োজনীয় ব্যান্ড ছাড়া ইমেজে অপারেশন।

  ```javascript
  // ভুল: ব্যান্ডহীন ইমেজে NDVI গণনার চেষ্টা
  var emptyImage = ee.Image([]); // কোনো ব্যান্ড নেই
  var ndvi = emptyImage.normalizedDifference(['B4', 'B5']); // এরর
  
  // সমাধান: আগে ব্যান্ড পরীক্ষা করুন
  var bandCheck = emptyImage.bandNames();
  bandCheck.evaluate(function(bands) {
    if (bands.length > 0) {
      var ndviCorrect = emptyImage.normalizedDifference(['B4', 'B5']);
    } else {
      print("NDVI গণনার জন্য কোনো ব্যান্ড নেই।");
    }
  });
  ```

---

# আর্থ ইঞ্জিনে প্রজেকশন

### ⚠️ সতর্কতা

- **স্বয়ংক্রিয় প্রজেকশন হ্যান্ডলিং**: আর্থ ইঞ্জিন ভিজ্যুয়ালাইজেশনের জন্য স্বয়ংক্রিয়ভাবে প্রজেকশন (যেমন- Web Mercator) নির্ধারণ করে। অপ্রত্যাশিত ফলাফল এড়াতে প্রজেকশন শুধুমাত্র প্রয়োজন হলেই স্পষ্টভাবে সেট করুন।
- **ডিফল্ট আউটপুট প্রজেকশন**: ভিন্ন প্রজেকশনযুক্ত ইমেজ একত্রিত করলে, ডিফল্ট আউটপুট হলো WGS84 (EPSG:4326) ১° স্কেলে, যা উচ্চ-রেজোলিউশন বিশ্লেষণের জন্য উপযুক্ত নাও হতে পারে। [https://epsg.io/](https://epsg.io/)
- **রিপ্রজেকশন ও ডেটা অনুরোধ**: `reproject()` ব্যবহার করার সময়, বর্তমান জুম লেভেলের চেয়ে সূক্ষ্ম স্কেলে ডেটা অনুরোধ করলে অতিরিক্ত ডেটা ব্যবহার হতে পারে।

### ❌ পরিহার করুন

- **প্রজেকশন অনির্দিষ্ট রাখা**: অসংগতি রোধে নিশ্চিত করুন প্রজেকশন সংজ্ঞায়িত আছে, বিশেষত `ImageCollection` এর সাথে।
- **ঘন ঘন রিপ্রজেকশন কল**: `reproject()` এর অতিরিক্ত ব্যবহার অপ্রয়োজনীয় রিপ্রজেকশনের কারণে কর্মক্ষমতা কমাতে পারে।
- **প্রজেকশন মিশানো**: একটি গণনায় ভিন্ন প্রজেকশনযুক্ত ইমেজ ব্যবহার করার সময়, এরর এড়াতে সেগুলো সঠিকভাবে হ্যান্ডেল করুন।

### 🤔 **নোট:**

- **প্রজেকশন তথ্য পরীক্ষা**: একটি ইমেজের প্রজেকশন জানতে `image.projection()` ব্যবহার করুন।

  ```javascript
  var image = ee.Image('LANDSAT/LC08/C02/T1_TOA/LC08_044034_20140318').select(0);
  print('Projection:', image.projection());
  print('Scale in meters:', image.projection().nominalScale());
  ```

- **কম্পোজিট ও প্রজেকশন:** ভিন্ন প্রজেকশনযুক্ত ইমেজ একত্রিত করলে ডিফল্ট WGS84 হয়।

  ```javascript
  var collection = ee.ImageCollection('LANDSAT/LC08/C02/T1_TOA');
  var mosaic = collection.filterDate('2018-01-01', '2019-01-01').mosaic();
  print(mosaic.projection());
  ```

- **প্রজেকশন নির্ধারণ:** নির্দিষ্ট প্রজেকশনের জন্য `reproject()` ব্যবহার করুন।

  ```javascript
  var proj = ee.Projection('EPSG:4326');
  var output = collection.reduce(...).reproject(proj);
  ```

---

### রিপ্রজেক্ট ও স্লোপ গণনার কোড উদাহরণ

- `ee.Image.reproject` ব্যবহার খুব কমই প্রয়োজন এবং সাধারণত `পরিহার` করা উচিত।
- যেখানে সম্ভব "scale", "crs", এবং "crsTransform" দিয়ে প্রজেকশন ও স্কেল সংজ্ঞায়িত করুন।

```javascript
// DEM ডেটাসেট লোড করুন এবং গড় উচ্চতা গণনা করুন
var dem1 = ee.Image('NASA/NASADEM_HGT/001').select('elevation');
var dem2 = ee.Image('CGIAR/SRTM90_V4').select('elevation');
var demMean = ee.ImageCollection([dem1, dem2]).mean();

Map.setCenter(-123.457, 47.815, 11);
Map.addLayer(dem1, {min: 500, max: 2500}, 'DEM 1');
Map.addLayer(demMean, {min: 500, max: 2500}, 'DEM composite');

// কম্পোজিট DEM থেকে ভূমি ঢাল গণনা করুন
var demCompSlope = ee.Terrain.slope(demMean);
Map.addLayer(demCompSlope, {min: 0, max: 0.3}, 'Slope');
```

```javascript
// reproject() দিয়ে ৩০মি স্কেলে ঢাল গণনা জোর করুন
var slopeScale = ee.Terrain.slope(
  demMean.reproject({crs: 'EPSG:4326', scale: 30})
);
Map.addLayer(slopeScale, {min: 0, max: 45}, 'Slope w/ CRS and scale');
```

---
🤔 নোট:
⚠️ সতর্কতা:
❌ পরিহার করুন:
